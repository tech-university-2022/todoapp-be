name: Build Docker Image

runs:
  steps:
    - name: Build image with docker
      shell: bash
      run: |
        docker build --build-arg ENV=$ENV --build-arg PORT=$PORT -t "todoapp" --no-cache .
    - name: Docker tag
      shell: bash
      run: |
        docker tag todoapp:latest todothucle.azurecr.io/todoapp
